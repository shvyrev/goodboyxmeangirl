/** *  * RTween Filter Module *  * @author Richard Rodney * @version 0.1 */package railk.as3.motion.modules {	import flash.filters.BitmapFilter;	import railk.as3.motion.utils.Prop;	public class FilterModule {		static public function update( target:Object, props:Prop, ratio:Number ):Prop {			var i:int = 0, p:String, filter:BitmapFilter = props.start.clone(), filterProps = props.end, filters:Array = target.filters, type:String = props.type;			for (p in filterProps) { filter[p] = filter[p]+(filterProps[p]-filter[p])*ratio; }			l:for (i=0; i<filters.length; i++) { if (filters[i].toString()=='[object '+type.charAt(0).toUpperCase()+type.substr(1,type.length)+'Filter]'){ filters.splice(i, 1); break l; } }			filters.push( filter );			target.filters = filters;			return props;		}	}}