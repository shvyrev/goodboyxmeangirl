/** * Snake Shape  *  * @author Richard Rodney * @version 0.1 */package railk.as3.display.graphicShape {		import flash.display.Bitmap;	import flash.display.BitmapData;	public class SnakeShape extends GraphicShape 	{		/**		 * CONSTRUCTEUR		 * 		 * @param	color		 * @param	radius		 * @param	size		 * @param	precision		 * @param	copy		 */		public function SnakeShape( color:uint=0x000000, radius:Number=10, innerRadius:Number=5, precision:int=200, copy:Boolean = false ):void {			super(copy);			_type = 'snake';			var rad:Number = (360/precision)*Math.PI/180, size:Number = radius-innerRadius;			this.graphicsCopy.clear();			for (var i:int = 0; i < precision; ++i) {				this.graphicsCopy.beginFill(color,1-(i*(1/precision)));				this.graphicsCopy.drawCircle(size+radius+Math.sin(rad*i)*radius, size+radius+Math.cos(rad*i)*radius, size);				this.graphicsCopy.endFill();				this.color = color;			}						var bmp:Bitmap = new Bitmap( new BitmapData(this.width, this.height, true, 0x00ffffff), 'never', true);			bmp.bitmapData.draw(this);			this.addChild(bmp);			this.graphicsCopy.clear();		}	}}